apiVersion: v1
kind: Service
metadata:
  name: {{ include "continental.fullname" . }}
  labels:
    {{- include "continental.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "continental.selectorLabels" . | nindent 4 }}
